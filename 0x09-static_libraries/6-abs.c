#include "main.h"

/**
 * _abs - a func that computes the absolute value
 * an integer
 * @x: paramter to compute.
 * Return: returns 0 if successful.
*/
int _abs(int x)
{
	int a = -1;

	if (x < 0)
		return (a * x);
	else
		return (x);
}
